<%- include("partials/header"); -%>

<h1>Hello</h1>
<p> <%= startingContent %> </p>

<% posts.forEach(function(post) { %>
  <h1><%= post.title %></h1>
  <% var content = post.post.substring(0,100) %>
  <% var hrefLink = "/posts/" + post.title; %>
  <p><%= content %> ... <a href="/posts/<%= post.title %>">Read More</a></p>

<% }); %>

<%- include("partials/footer"); -%>
